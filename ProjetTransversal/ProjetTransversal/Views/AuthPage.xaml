<Page
    x:Class="ProjetTransversal.Views.AuthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjetTransversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=AuthPage}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Please enter your PIN here:" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding AuthPin, Mode=TwoWay}"/>
            <Button Content="Enter" Command="{Binding Login}"/>
        </StackPanel>
        <WebView x:Name="AuthWeb" Grid.Row="1" Source="{Binding AuthURL}"/>
    </Grid>
</Page>
